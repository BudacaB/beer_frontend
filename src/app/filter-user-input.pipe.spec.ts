import { FilterUserInputPipe } from './filter-user-input.pipe';

describe('FilterUserInputPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterUserInputPipe();
    expect(pipe).toBeTruthy();
  });
});
